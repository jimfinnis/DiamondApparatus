Client to server

long: type
followed by

type 1: publish value   
    char[256]: name
    long: count (number of values) (long for padding purposes!)
    sizeof(float)*count: data
    
type 2: subscribe to value
    char[256]: name

type 3: kill server

Server to client

long: type
followed by

type 10: acknowledged
    byte: 0 or error code
    char[256]: error message
    
type 20: subscription notification
    char[256]:name
    short: count (number of values)
    sizeof(float)*count: data
            
